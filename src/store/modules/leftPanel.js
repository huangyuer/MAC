import { levelOneCategoryItem } from '../objectDeclare'

const state = {
  leftPanelItemList: [
    {
      name: '没有找到相关文献',
      children: []
    }
  ],
}

const actions = {}

const mutations = {}

export default {
  state,
  actions,
  mutations,
}
