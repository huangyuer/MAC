const state = {
  searchContextData: []
}

const actions = {}

const mutations = {
  setSearchContextData (state, data) {
    state.searchContextData = data
  }
}

export default {
  state,
  actions,
  mutations,
}
